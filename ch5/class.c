#include <stdio.h>

int main(){
    /*
    int i = 100;
    do{
        i++;
    }while(i<110);
    printf("%d", i);
    */

   /*
   int a[3] = {1, 3, 5 };
   int b[3] = {4, -2, -1};
   int c = 0;
   for (int i = 0; i < 3; i++ ){
        c += a[i]*b[i];
   }
   printf("%d", c);
   */
  /*
    int fab[100] = {0, 1};
    int n = 15;
    for (int i = 2; i<n; i++){
        fab[i] = fab[i-1]+fab[i-2];
    }

    for (int i = 0; i < n; i++){
        printf("%d\n", fab[i]);
    }
   
   int i = 0;
    while ((i < n)&&(fab[i]!=35))
        i++;
    if (i == n)
        printf("\nnot found yaaaæaaa");
    else
    */
   

}
