#include <stdio.h>
#include <string.h>

struct data
{
    string med;
    int time;
};

int main()
{
    int t;
    scanf("%d", &t);
    for (int i = 0; i < t; i++)
    {
        double n, k;
        scanf("%ld%ld", n, k);
        
    }
}